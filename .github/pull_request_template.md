**What this changes and why**

**Test that fails before / passes after**

- [ ] `npm test` and `npm run typecheck` pass
- [ ] Touches `src/providers/` → `npm run e2e` run
- [ ] Touches `src/extension.ts` → noted as a breaking change for Pro
- [ ] Docs updated if behaviour changed (`docs/security.md` must stay true)
